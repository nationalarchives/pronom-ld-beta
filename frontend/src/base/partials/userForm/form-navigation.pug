block vars
  - var currentFormSectionw = 1

aside
  .form-nav-container
    .border &nbsp;
    ul.main-nav
      li.active
        button.segment(onclick=" currentFormSectionw = 2")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Core
      li
        button.segment(onclick="showFormSection(2)")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Signature
      li.from-external-only
        button.segment.nested(onclick="showFormSection(3)")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span More information
        ul.side-menu
          li
            button(onclick="showFormSection(3)") Priority
          li
            button(onclick="showFormSection(4)") Identifiers
          li
            button(onclick="showFormSection(5)") Relationship
          li
            button(onclick="showFormSection(6)") Additional properties
      li.from-internal-only
        button.segment(onclick="showFormSection(4)")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Relationships
      li.from-internal-only
        button.segment(onclick="showFormSection(5)")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Identifiers
      li.from-internal-only
        button.segment(onclick="showFormSection(6)")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Additional properties
      li.from-internal-only
        button.segment
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Contributor
      li.from-external-only
        button.segment(onclick="showFormSection(7)")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Your details
      li
        button.segment(onclick="showFormSection(8)")
          .top-button
            .line &nbsp;
            .round &nbsp;
            span Review

script.
  var currentFormSectionw = 0;
  function showFormSection(formPart){
    var currentFormSection = formPart;
    console.log(currentFormSection);
    return var currentFormSection
  }