section.form-part#signatures
  .smallScreen
    button.accordion
      i.fa-solid.fa-chevron-down
      h2 Signatures
  .largeScreen
    .form-header-main
      h2 Signatures
      .nav
        button.bordered-blue.medium.left.prev
          i.fa-solid.fa-angles-left
          | Prev
        button.bordered-blue.medium.right.next
          | Next
          i.fa-solid.fa-angles-right
  .form-partial-content
      //- signature select
      .part
        fieldset
          legend.sr-only Select signature type you wish to add
          #signature-controls
            #select-signature
              div
                .tooltip-container
                  .label
                    label(for="select-signature-type") Select signature type
                    span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.1.1') tabindex='0')
                      .i-wrapper
                        i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                div
                  select(id="select-signature-type")
                    option(value='signature') Binary Signature
                    option(value='container-signature') Container Signature
              .button-container
                button.black.small#add-signature
                  | Add signature
            .collapse-buttons
              button.bordered-blue.small.open-all-signatures
                | Open all
                i.fa-solid.fa-chevron-down
              button.bordered-blue.small.collapse-all-signatures
                | Collapse all
                i.fa-solid.fa-chevron-up
        //- Signature 
        #signature-container
          .holder
            .row-group-inner1.path.template
              .button-container 
                button.bordered-red.small.push-right.delete-path
                  | Delete path

              //- Path (hidden for copying) =============================================
              .form-row
                .tooltip-container
                  .label
                    label.from-external-only(for="Path") Path
                    label.from-internal-only(for="Path") Path (required)
                    span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.7') tabindex='0')
                      .i-wrapper
                        i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                input(id="Path", name="containerSignatures[0].files[0].path" disabled)
              .byte-sequence-list
                .list
                .button-container
                  button.black.small.add-byte-sequence.push-right
                    | Add byte sequence

            //- Byte sequence (hidden for copying) =============================================
            .row-group-inner2.byte-sequence.template
              .info
                h6 Byte sequence
                button.bordered-red.small.delete-byte-sequence
                  | Delete byte sequence

              //- Binary value
              .form-row.binary-value
                p.inputType Edited content
                .tooltipPlusAction
                  .tooltip-container
                    .label
                      label.from-external-only(for="BinaryValue") Binary value
                      label.from-internal-only(for="BinaryValue") Binary value (required)
                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.2.3') tabindex='0')
                        .i-wrapper
                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                  .action-monitoring
                    .review-type
                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                      p Require reviewing
                    .review-type
                      i.fa-solid.fa-square-check
                      p Reviewed
                .input-group
                  button.current Accept current
                  input(id="BinaryValue", disabled)

              //- Position type
              .form-rowposition-type
                p.inputType Edited content
                .tooltipPlusAction
                  .tooltip-container
                    .label
                      label.from-external-only(for="PositionType") Position type
                      label.from-internal-only(for="PositionType") Position type (required)
                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.4') tabindex='0')
                        .i-wrapper
                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                  .action-monitoring
                    .review-type
                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                      p Require reviewing
                    .review-type
                      i.fa-solid.fa-square-check
                      p Reviewed
                .input-group
                  button.current Accept current
                  select(id="PositionType", disabled)
                    option(value='0') option1
                    option(value='1') option2

              //- Min offset
              .form-row.min-offset
                p.inputType Edited content
                .tooltipPlusAction
                  .tooltip-container
                    .label
                      label.from-external-only(for="MinOffset") Min offset
                      label.from-internal-only(for="MinOffset") Min offset (required)
                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.5') tabindex='0')
                        .i-wrapper
                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                  .action-monitoring
                    .review-type
                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                      p Require reviewing
                    .review-type
                      i.fa-solid.fa-square-check
                      p Reviewed
                .input-group
                  button.current Accept current
                  input.short(name="MinOffset", id="MinOffset", disabled)

              //- Max offset
              .form-row.max-offset
                p.inputType Edited content
                .tooltipPlusAction
                  .tooltip-container
                    .label
                      label.from-external-only(for="MaxOffset") Max offset
                      label.from-internal-only(for="MaxOffset") Max offset (required)
                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.6') tabindex='0')
                        .i-wrapper
                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                  .action-monitoring
                    .review-type
                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                      p Require reviewing
                    .review-type
                      i.fa-solid.fa-square-check
                      p Reviewed
                .input-group
                  button.current Accept current
                  input.short(name="MaxOffset", id="MaxOffset", disabled)
            //- template signature (hidden for copying) =============================================
            fieldset.signature.template.collapse-content-container
              legend.sr-only Fill up information about signature
              .row-group
                .top
                  button.accordion
                    .info
                      p Type: Signature
                      p(th:unless="${#strings.isEmpty(ff?.puid)}", th:text="${'PUID: '+ ff?.puid}")
                    i.fa-solid.fa-chevron-up
                  button.bordered-red.small.delete-signature Delete signature
                .collapse-content

                  //- Signature name
                  .form-row.signature-name
                    .tooltip-container
                      .label
                        label.from-external-only(for="signatureName") Signature name
                        label.from-internal-only(for="signatureName") Signature name (required)
                        span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.3.1') tabindex='0')
                          .i-wrapper
                            i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                    input(name="internalSignatures[0].name", id="signatureName", disabled)

                  h3 Byte Sequences
                  .part

                    //- //- Select endianess
                    .form-row.byte-order
                      .tooltip-container
                        .label
                          label.from-external-only(for="endianess") Select endianess
                          label.from-internal-only(for="endianess") Select endianess (required)
                          span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.1') tabindex='0')
                            .i-wrapper
                              i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                      select(name="internalSignatures[0].byteOrder", id="endianess")
                        option(th:value="null") None
                        option(th:each="bo : ${byteOrderOptions}", th:value="${bo.URI}", th:text="${bo.label}") option 1

                    //- Description
                    .form-row.signature-description
                      p.inputType Edited content
                      .tooltipPlusAction
                        .tooltip-container
                          .label
                            label.from-external-only(for="SignatureDescription") Description
                            label.from-internal-only(for="SignatureDescription") Description (required)
                            span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.2') tabindex='0')
                              .i-wrapper
                                i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                        .action-monitoring
                          .review-type
                            i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                            p Require reviewing
                          .review-type
                            i.fa-solid.fa-square-check
                            p Reviewed
                      .input-group
                        button.current Accept current
                        textarea(cols="30", rows="6", name="internalSignatures[0].note", id="SignatureDescription", disabled)
                    //- byte sequence
                    .byte-sequence-list
                      .list
                        .row-group-inner2.byte-sequence
                          .info
                            h4 Byte sequence
                            button.bordered-red.small.delete-byte-sequence
                              | Delete byte sequence

                          //- Binary value
                          .form-row.binary-value
                            p.inputType Edited content
                            .tooltipPlusAction
                              .tooltip-container
                                .label
                                  label.from-external-only(for="binaryValue") Binary value
                                  label.from-internal-only(for="binaryValue") Binary value (required)
                                  span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.2.3') tabindex='0')
                                    .i-wrapper
                                      i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                              .action-monitoring
                                .review-type
                                  i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                  p Require reviewing
                                .review-type
                                  i.fa-solid.fa-square-check
                                  p Reviewed
                            .input-group
                              button.current Accept current
                              input(name="internalSignatures[0].byteSequences[0].sequence", id="binaryValue", disabled)

                          //- Position type
                          .form-row.position-type
                            p.inputType Edited content
                            .tooltipPlusAction
                              .tooltip-container
                                .label
                                  label.from-external-only(for="positionType") Position type
                                  label.from-internal-only(for="positionType") Position type (required)
                                  span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.4') tabindex='0')
                                    .i-wrapper
                                      i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                              .action-monitoring
                                .review-type
                                  i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                  p Require reviewing
                                .review-type
                                  i.fa-solid.fa-square-check
                                  p Reviewed
                            .input-group
                              button.current Accept current
                              select(name="internalSignatures[0].byteSequences[0].position", id="positionType", disabled)
                                option(th:each="po : ${positionTypeOptions}", th:value="${po.URI}", th:text="${po.label}") option 1

                          //- Min offset
                          .form-row.min-offset
                            p.inputType Edited content
                            .tooltipPlusAction
                              .tooltip-container
                                .label
                                  label.from-external-only(for="minOffset") Min offset
                                  label.from-internal-only(for="minOffset") Min offset (required)
                                  span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.5') tabindex='0')
                                    .i-wrapper
                                      i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                              .action-monitoring
                                .review-type
                                  i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                  p Require reviewing
                                .review-type
                                  i.fa-solid.fa-square-check
                                  p Reviewed
                            .input-group
                              button.current Accept current
                              input.short(name="internalSignatures[0].byteSequences[0].offset", id="minOffset", disabled)

                          //- Max offset
                          .form-row.max-offset
                            p.inputType Edited content
                            .tooltipPlusAction
                              .tooltip-container
                                .label
                                  label.from-external-only(for="maxOffset") Max offset
                                  label.from-internal-only(for="maxOffset") Max offset (required)
                                  span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.6') tabindex='0')
                                    .i-wrapper
                                      i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                              .action-monitoring
                                .review-type
                                  i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                  p Require reviewing
                                .review-type
                                  i.fa-solid.fa-square-check
                                  p Reviewed
                            .input-group
                              button.current Accept current
                              input.short(name="internalSignatures[0].byteSequences[0].maxOffset", id="maxOffset", disabled)
                      .button-container
                        button.black.small.add-byte-sequence.push-right Add byte sequence
            //- Container-signature (hidden for copying) =============================================
            fieldset.container-signature.template.collapse-content-container
              legend.sr-only Fill up information about signature
              .row-group
                .top
                  button.accordion
                    .info
                      p Type: Container Signature
                      p(th:unless="${#strings.isEmpty(ff?.puid)}", th:text="${'PUID: '+ ff?.puid}")
                    i.fa-solid.fa-chevron-up
                  button.bordered-red.small.delete-container-signature
                    | Delete container signature
                .collapse-content

                  //- Container signature name
                  .form-row
                    .tooltip-container
                      .label
                        label.from-external-only(for="containerSignatureName") Signature name
                        label.from-internal-only(for="containerSignatureName") Signature name (required)
                        span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.3.1') tabindex='0')
                          .i-wrapper
                            i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                    input(name="containerSignatures[0].name", id="containerSignatureName", disabled)
                  
                  //- //- Select endianess
                  .form-row.container-type
                    .tooltip-container
                      .label
                        label(for="container-type") Select container type
                        span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.1') tabindex='0')
                          .i-wrapper
                            i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                    select(name="containerSignature[0].containerType", id="container-type")
                      option(th:value="null") None
                      option(th:each="cto : ${containerTypeOptions}", th:value="${cto.URI}", th:text="${cto.label}") option 1

                  h5.border Byte Sequences
                  .part
                    //- fieldset
                    //-   legend.sr-only Select endianess
                    //-   div
                    //-     .tooltip-container
                    //-       .label
                    //-         label.from-external-only(for="CSendianess") Select endianess
                    //-         label.from-internal-only(for="CSendianess") Select endianess (required)
                    //-         span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.1') tabindex='0')
                    //-           .i-wrapper
                    //-             i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                    //-     select(id="CSendianess")
                    //-       option(value='0') option1
                    //-       option(value='1') option2

                    //- Description
                    .form-row
                      .tooltip-container
                        .label
                            label.from-external-only(for="signatureDescription") Description
                            label.from-internal-only(for="signatureDescription") Description (required)
                            span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.2') tabindex='0')
                              .i-wrapper
                                i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                      textarea(cols="30", rows="6", name="containerSignatures[0].description", id="signatureDescription", disabled)
                    h5.border Container signature
                    .paths-list-container
                      .paths-list
                      .button-container
                        button.black.small.push-right.add-path
                          | Add path
          
          //- simple signature templated output:
          fieldset.signature.collapse-content-container(th:each="sig, stat : *{internalSignatures}", th:with="compareSize=${#lists.size(compare?.internalSignatures ?: {})}, compareSig=${compareSize > stat.index} ? ${compare.internalSignatures.get(__${stat.index}__)} : null")
            input(type="hidden", th:field="*{internalSignatures[__${stat.index}__].uri}")
            legend.sr-only Fill up information about signature
            .row-group
              .top
                button.accordion
                  .info
                    p Type: Signature
                    p(th:unless="${#strings.isEmpty(ff?.puid)}", th:text="${'PUID: '+ ff?.puid}") PUID: (some PUID if any)
                  i.fa-solid.fa-chevron-up
                button.bordered-red.small.delete-signature Delete signature
              .collapse-content

                //- Signature name
                .form-row.signature-name(th:classappend="${compareSig?.name != null && compareSig?.name != sig?.name} ? 'before-review' : ''")
                  p.inputType(th:text="${#strings.isEmpty(ff?.internalSignatures?.get(__${stat.index}__)?.name) ? 'New content' : 'Edited content'}") Edited content
                  .tooltipPlusAction
                    .tooltip-container
                      .label
                        label.from-external-only(th:for="${'SignatureName' + stat.index}") Signature name
                        label.from-internal-only(th:for="${'SignatureName' + stat.index}") Signature name (required)
                        span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.3.1') tabindex='0')
                          .i-wrapper
                            i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                    .action-monitoring
                      .review-type
                        i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                        p Require reviewing
                      .review-type
                        i.fa-solid.fa-square-check
                        p Reviewed
                  .input-group
                    button.current Accept current    
                    input(th:field="*{internalSignatures[__${stat.index}__].name}", th:id="${'SignatureName' + stat.index}", th:data-undo="${ff?.internalSignatures?.get(__${stat.index}__)?.name}")
                  .input-group(th:if="${compareSig?.name != null && compareSig?.name != sig?.name}")
                    button.incoming Accept incoming
                    input(th:value="${compareSig.name}", th:id="${'SignatureName' + stat.index}")

                h3 Byte Sequences
                .part
                  //- Description
                  .form-row.signature-description(th:classappend="${compareSig?.note != null && compareSig?.note != sig?.note} ? 'before-review' : ''")
                    p.inputType(th:text="${#strings.isEmpty(ff?.internalSignatures?.get(__${stat.index}__)?.note) ? 'New content' : 'Edited content'}") Edited content
                    .tooltipPlusAction
                      .tooltip-container
                        .label
                          label.from-external-only(th:for="${'SignatureNote' + stat.index}") Description
                          label.from-internal-only(th:for="${'SignatureNote' + stat.index}") Description (required)
                          span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.2.2') tabindex='0')
                            .i-wrapper
                              i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                      .action-monitoring
                        .review-type
                          i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                          p Require reviewing
                        .review-type
                          i.fa-solid.fa-square-check
                          p Reviewed
                    .input-group
                      button.current Accept current
                      textarea(cols="30", rows="6" th:field="*{internalSignatures[__${stat.index}__].note}", th:id="${'SignatureNote' + stat.index}", th:data-undo="${ff?.internalSignatures?.get(__${stat.index}__)?.note}")
                    .input-group(th:if="${compareSig?.note != null && compareSig?.note != sig?.note}")
                      button.incoming Accept incoming
                      textarea(cols="30", rows="6", th:value="${compare.internalSignatures?.get(__${stat.index}__).note}", th:id="${'SignatureNote' + stat.index}")
                  //- byte sequence
                  .byte-sequence-list
                    .list
                      .row-group-inner2.byte-sequence(th:each="byteSeq, statB : *{internalSignatures.get(__${stat.index}__).byteSequences}", th:with="compareBSSize=${#lists.size(compareSig?.byteSequences ?: {})}, compareByteSeq=${compareBSSize > statB.index} ? ${compareSig.byteSequences.get(__${statB.index}__)} : null")
                        .info
                          h4 Byte sequence
                          button.bordered-red.small.delete-byte-sequence
                            | Delete byte sequence
                          
                        //- Binary value
                        .form-row.binary-value(th:classappend="${compareByteSeq?.sequence != null && compareByteSeq?.sequence != byteSeq?.sequence} ? 'before-review' : ''")
                          p.inputType(th:text="${#strings.isEmpty(byteSeq?.sequence) ? 'New content' : 'Edited content'}") Edited content
                          .tooltipPlusAction
                            .tooltip-container
                              .label
                                label.from-external-only(th:for="${'ByteSequenceSignature' + stat.index + '-' + statB.index}") Binary value
                                label.from-internal-only(th:for="${'ByteSequenceSignature' + stat.index + '-' + statB.index}") Binary value (required)
                                span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.2.3') tabindex='0')
                                  .i-wrapper
                                    i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                            .action-monitoring
                              .review-type
                                i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                p Require reviewing
                              .review-type
                                i.fa-solid.fa-square-check
                                p Reviewed
                          .input-group
                            button.current Accept current
                            input(th:field="*{internalSignatures[__${stat.index}__].byteSequences[__${statB.index}__].sequence}", th:id="${'ByteSequenceSignature' + stat.index + '-' + statB.index}")
                          .input-group(th:if="${compareByteSeq?.sequence != null && compareByteSeq?.sequence != byteSeq?.sequence}")
                            button.incoming Accept incoming
                            input(th:value="${compareByteSeq?.sequence}", th:id="${'ByteSequenceSignature' + stat.index + '-' + statB.index}")

                        //- Byte Order
                        .form-row.position-type(th:classappend="${compareByteSeq?.byteOrder != null && compareByteSeq?.byteOrder != compareByteSeq?.byteOrder} ? 'before-review' : ''")
                          p.inputType(th:text="${#strings.isEmpty(byteSeq?.byteOrder) ? 'New content' : 'Edited content'}") Edited content
                          .tooltipPlusAction
                            .tooltip-container
                              .label
                                label.from-external-only(th:for="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index}") Byte Order
                                label.from-internal-only(th:for="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index}") Byte Order (required)
                                span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.4') tabindex='0')
                                  .i-wrapper
                                    i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                            .action-monitoring
                              .review-type
                                i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                p Require reviewing
                              .review-type
                                i.fa-solid.fa-square-check
                                p Reviewed
                          .input-group
                            button.current Accept current
                            select(th:field="*{internalSignatures[__${stat.index}__].byteSequences[__${statB.index}__].byteOrder}", th:id="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index}")
                              option(value='null') None
                              option(th:each="bo : ${byteOrderOptions}", th:value="${bo.URI}", th:text="${bo.label}") option 1
                          .input-group(th:if="${compareByteSeq?.byteOrder != null && compareByteSeq?.byteOrder != byteSeq?.byteOrder}")
                            button.incoming Accept incoming
                            select(th:value="${compareByteSeq?.byteOrder}", th:id="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index}")
                              option(value='null') None
                              option(th:each="bo : ${byteOrderOptions}", th:value="${bo.URI}", th:text="${bo.label}") option 1

                        //- Position type
                        .form-row.position-type(th:classappend="${compareByteSeq?.position != null && compareByteSeq?.position != byteSeq?.position} ? 'before-review' : ''")
                          p.inputType(th:text="${#strings.isEmpty(byteSeq?.position) ? 'New content' : 'Edited content'}") Edited content
                          .tooltipPlusAction
                            .tooltip-container
                              .label
                                label.from-external-only(th:for="${'ByteSequencePosition' + stat.index + '-' + statB.index}") Position type
                                label.from-internal-only(th:for="${'ByteSequencePosition' + stat.index + '-' + statB.index}") Position type (required)
                                span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.4') tabindex='0')
                                  .i-wrapper
                                    i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                            .action-monitoring
                              .review-type
                                i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                p Require reviewing
                              .review-type
                                i.fa-solid.fa-square-check
                                p Reviewed
                          .input-group
                            button.current Accept current
                            select(th:field="*{internalSignatures[__${stat.index}__].byteSequences[__${statB.index}__].position}", th:id="${'ByteSequencePosition' + stat.index + '-' + statB.index}")
                              option(th:each="po : ${positionTypeOptions}", th:value="${po.URI}", th:text="${po.label}") option 1
                          .input-group(th:if="${compareByteSeq?.position != null && compareByteSeq?.position != byteSeq?.position}")
                            button.incoming Accept incoming
                            select(th:value="${compareByteSeq?.position}", th:id="${'ByteSequencePosition' + stat.index + '-' + statB.index}")
                              option(th:each="po : ${positionTypeOptions}", th:value="${po.URI}", th:text="${po.label}") option 1

                        //- Min offset
                        .form-row.min-offset(th:classappend="${compareByteSeq?.offset != null && compareByteSeq?.offset != byteSeq?.offset} ? 'before-review' : ''")
                          p.inputType(th:text="${#strings.isEmpty(byteSeq?.offset) ? 'New content' : 'Edited content'}") Edited content
                          .tooltipPlusAction
                            .tooltip-container
                              .label
                                label.from-external-only(th:for="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index}") Min offset
                                label.from-internal-only(th:for="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index}") Min offset (required)
                                span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.5') tabindex='0')
                                  .i-wrapper
                                    i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                            .action-monitoring
                              .review-type
                                i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                p Require reviewing
                              .review-type
                                i.fa-solid.fa-square-check
                                p Reviewed
                          .input-group
                            button.current Accept current
                            input.short(th:field="*{internalSignatures[__${stat.index}__].byteSequences[__${statB.index}__].offset}", th:id="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index}")
                          .input-group(th:if="${compareByteSeq?.offset != null && compareByteSeq?.offset != byteSeq?.offset}")
                            button.incoming Accept incoming
                            input.short(th:value="${compareByteSeq?.offset}", th:id="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index}")
                        
                        //- Max offset
                        .form-row.max-offset(th:classappend="${compareByteSeq?.offset != null && compareByteSeq?.offset != byteSeq?.offset} ? 'before-review' : ''")
                          p.inputType(th:text="${#strings.isEmpty(byteSeq?.maxOffset) ? 'New content' : 'Edited content'}") Edited content
                          .tooltipPlusAction
                            .tooltip-container
                              .label
                                label.from-external-only(th:for="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index}") Max offset
                                label.from-internal-only(th:for="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index}") Max offset (required)
                                span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.6') tabindex='0')
                                  .i-wrapper
                                    i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                            .action-monitoring
                              .review-type
                                i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                p Require reviewing
                              .review-type
                                i.fa-solid.fa-square-check
                                p Reviewed
                          .input-group
                            button.current Accept current
                            input.short(th:field="*{internalSignatures[__${stat.index}__].byteSequences[__${statB.index}__].maxOffset}", th:id="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index}")
                          .input-group(th:if="${compareByteSeq?.maxOffset != null && compareByteSeq?.maxOffset != byteSeq?.maxOffset}")
                            button.incoming Accept incoming
                            input.short(th:value="${compareByteSeq?.maxOffset}" , th:id="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index}")
                    .button-container
                      button.black.small.add-byte-sequence.push-right Add byte sequence

          //- container signature templated output
          fieldset.container-signature.collapse-content-container(th:each="sig, stat : *{containerSignatures}", th:with="compareSize=${#lists.size(compare?.containerSignatures ?: {})}, compareSig=${compareSize > stat.index} ? ${compare.containerSignatures.get(__${stat.index}__)} : null")
            input(type="hidden", th:field="*{containerSignatures[__${stat.index}__].uri}")
            legend.sr-only Fill up information about signature
            .row-group
              .top
                button.accordion
                  .info
                    p Type: Container Signature
                    p(th:unless="${#strings.isEmpty(ff?.puid)}", th:text="${'PUID: '+ ff?.puid}")
                  i.fa-solid.fa-chevron-up
                button.bordered-red.small.delete-container-signature
                  | Delete container signature
              .collapse-content
                //- Container signature name
                .form-row(th:classappend="${compareSig?.name != null && compareSig?.name != sig?.name} ? 'before-review' : ''")
                  p.inputType(th:text="${#strings.isEmpty(sig?.name) ? 'New content' : 'Edited content'}") Edited content
                  .tooltip-container
                    .label
                      label.from-external-only(for="${'containerSignatureName' + stat.index}") Signature name
                      label.from-internal-only(for="${'containerSignatureName' + stat.index}") Signature name (required)
                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.3.1') tabindex='0')
                        .i-wrapper
                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                  input(th:field="*{containerSignatures[__${stat.index}__].name}", id="${'containerSignatureName' + stat.index}")
                //- Select Container type
                .form-row.container-type
                  .tooltip-container
                    .label
                      label(for="${'containerSignatureType' + stat.index}") Select container type
                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.1') tabindex='0')
                        .i-wrapper
                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                  select(th:field="*{containerSignatures[__${stat.index}__].containerType}" id="${'containerSignatureType' + stat.index}")
                    option(th:value="null") None
                    option(th:each="cto : ${containerTypeOptions}", th:value="${cto.URI}", th:text="${cto.label}") option 1
                h5.border Byte Sequences
                .part
                  //- fieldset
                  //-   legend.sr-only Select endianess
                  //-   div
                  //-     .tooltip-container
                  //-       .label
                  //-         label.from-external-only(for="CSendianess") Select endianess
                  //-         label.from-internal-only(for="CSendianess") Select endianess (required)
                  //-         span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.1') tabindex='0')
                  //-           .i-wrapper
                  //-             i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                  //-     select(id="CSendianess")
                  //-       option(value='0') option1
                  //-       option(value='1') option2
                  //- Description
                  .form-row
                    .tooltip-container
                      .label
                          label.from-external-only(for="signatureDescription") Description
                          label.from-internal-only(for="signatureDescription") Description (required)
                          span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.2') tabindex='0')
                            .i-wrapper
                              i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                    textarea(cols="30", rows="6", th:field="*{containerSignatures[__${stat.index}__].description}" id="signatureDescription")
                  h5.border Container signature
                  .paths-list-container
                    .paths-list
                      //- Paths templated output
                      .row-group-inner1.path.template
                        .button-container 
                          button.bordered-red.small.push-right.delete-path
                            | Delete path
                        .form-row(th:each="file, statB : ${sig.files}", th:with="compareFileSize=${#lists.size(compareSig?.files ?: {})}, compareFile=${compareFileSize > statB.index} ? ${compareSig.files.get(__${statB.index}__)} : null")
                          .tooltip-container
                            .label
                              label.from-external-only(for="${'Path'+stat.index+'-'+statB.index}") Path
                              label.from-internal-only(for="${'Path'+stat.index+'-'+statB.index}") Path (required)
                              span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.7') tabindex='0')
                                .i-wrapper
                                  i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                          input(th:id="${'Path'+stat.index+'-'+statB.index}", th:field="*{containerSignatures[__${stat.index}__].files[__${statB.index}__].path}")
                        .byte-sequence-list
                          .list
                            .row-group-inner2.byte-sequence(th:each="byteSeq, statC : ${file?.byteSequences ?: {}}", th:with="compareBSSize=${#lists.size(compareFile?.byteSequences ?: {})}, compareByteSeq=${compareBSSize > statC.index} ? ${compareFile.byteSequences.get(__${statC.index}__)} : null")
                              .info
                                h4 Byte sequence
                                button.bordered-red.small.delete-byte-sequence
                                  | Delete byte sequence
                              
                              //- Binary value
                              .form-row.binary-value(th:classappend="${compareByteSeq?.sequence != null && compareByteSeq?.sequence != byteSeq?.sequence} ? 'before-review' : ''")
                                p.inputType(th:text="${#strings.isEmpty(byteSeq?.sequence) ? 'New content' : 'Edited content'}") Edited content
                                .tooltipPlusAction
                                  .tooltip-container
                                    .label
                                      label.from-external-only(th:for="${'ByteSequenceSignature' + stat.index + '-' + statB.index + '-' + statC.index}") Binary value
                                      label.from-internal-only(th:for="${'ByteSequenceSignature' + stat.index + '-' + statB.index + '-' + statC.index}") Binary value (required)
                                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.2.3') tabindex='0')
                                        .i-wrapper
                                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                                  .action-monitoring
                                    .review-type
                                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                      p Require reviewing
                                    .review-type
                                      i.fa-solid.fa-square-check
                                      p Reviewed
                                .input-group
                                  button.current Accept current
                                  input(th:field="*{containerSignatures[__${stat.index}__].files[__${statB.index}__].byteSequences[__${statC.index}__].sequence}", th:id="${'ByteSequenceSignature' + stat.index + '-' + statB.index + '-' + statC.index}")
                                .input-group(th:if="${compareByteSeq?.sequence != null && compareByteSeq?.sequence != byteSeq?.sequence}")
                                  button.incoming Accept incoming
                                  input(th:value="${compareByteSeq.sequence}", th:id="${'ByteSequenceSignature' + stat.index + '-' + statB.index + '-' + statC.index}")
                              
                              //- Byte Order
                              .form-row.position-type(th:classappend="${compareByteSeq?.byteOrder != null && compareByteSeq?.byteOrder != byteSeq?.byteOrder} ? 'before-review' : ''")
                                p.inputType(th:text="${#strings.isEmpty(byteSeq?.byteOrder)) ? 'New content' : 'Edited content'}") Edited content
                                .tooltipPlusAction
                                  .tooltip-container
                                    .label
                                      label.from-external-only(th:for="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index + '-' + statC.index}") Position type
                                      label.from-internal-only(th:for="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index + '-' + statC.index}") Position type (required)
                                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.4') tabindex='0')
                                        .i-wrapper
                                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                                  .action-monitoring
                                    .review-type
                                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                      p Require reviewing
                                    .review-type
                                      i.fa-solid.fa-square-check
                                      p Reviewed
                                .input-group
                                  button.current Accept current
                                  select(th:field="*{containerSignatures[__${stat.index}__].files[__${statB.index}__].byteSequences[__${statC.index}__].byteOrder}", th:id="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index + '-' + statC.index}")
                                    option(value='null') None
                                    option(th:each="bo : ${byteOrderOptions}", th:value="${bo.URI}", th:text="${bo.label}") option 1
                                .input-group(th:if="${compareByteSeq?.byteOrder != null && compareByteSeq?.byteOrder != byteSeq?.byteOrder}")
                                  button.incoming Accept incoming
                                  select(th:value="${compareByteSeq?.byteOrder}", th:id="${'ByteSequenceByteOrder' + stat.index + '-' + statB.index + '-' + statC.index}")
                                    option(value='null') None
                                    option(th:each="bo : ${byteOrderOptions}", th:value="${bo.URI}", th:text="${bo.label}") option 1
                              
                              //- Position type
                              .form-row.position-type(th:classappend="${compareByteSeq?.position != null && compareByteSeq?.position != byteSeq?.position} ? 'before-review' : ''")
                                p.inputType(th:text="${#strings.isEmpty(byteSeq?.position) ? 'New content' : 'Edited content'}") Edited content
                                .tooltipPlusAction
                                  .tooltip-container
                                    .label
                                      label.from-external-only(th:for="${'ByteSequencePosition' + stat.index + '-' + statB.index}") Position type
                                      label.from-internal-only(th:for="${'ByteSequencePosition' + stat.index + '-' + statB.index}") Position type (required)
                                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.4') tabindex='0')
                                        .i-wrapper
                                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                                  .action-monitoring
                                    .review-type
                                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                      p Require reviewing
                                    .review-type
                                      i.fa-solid.fa-square-check
                                      p Reviewed
                                .input-group
                                  button.current Accept current
                                  select(th:field="*{containerSignatures[__${stat.index}__].files[__${statB.index}__].byteSequences[__${statC.index}__].position}", th:id="${'ByteSequencePosition' + stat.index + '-' + statB.index + '-' + statC.index}")
                                    option(th:each="po : ${positionTypeOptions}", th:value="${po.URI}", th:text="${po.label}") option 1
                                .input-group(th:if="${compareByteSeq?.position != null && compareByteSeq?.position != compareByteSeq?.position}")
                                  button.incoming Accept incoming
                                  select(th:value="${compareByteSeq.position != null && compareByteSeq?.position != byteSeq?.position}", th:id="${'ByteSequencePosition' + stat.index + '-' + statB.index + '-' + statC.index}")
                                    option(th:each="po : ${positionTypeOptions}", th:value="${po.URI}", th:text="${po.label}") option 1
                              
                              //- Min offset
                              .form-row.min-offset(th:classappend="${compareByteSeq?.offset != null && compareByteSeq?.offset != byteSeq?.offset} ? 'before-review' : ''")
                                p.inputType(th:text="${#strings.isEmpty(byteSeq?.offset) ? 'New content' : 'Edited content'}") Edited content
                                .tooltipPlusAction
                                  .tooltip-container
                                    .label
                                      label.from-external-only(th:for="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index + '-' + statC.index}") Min offset
                                      label.from-internal-only(th:for="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index + '-' + statC.index}") Min offset (required)
                                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.5') tabindex='0')
                                        .i-wrapper
                                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                                  .action-monitoring
                                    .review-type
                                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                      p Require reviewing
                                    .review-type
                                      i.fa-solid.fa-square-check
                                      p Reviewed
                                .input-group
                                  button.current Accept current
                                  input.short(th:field="*{containerSignatures[__${stat.index}__].files[__${statB.index}__].byteSequences[__${statC.index}__].offset}", th:id="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index + '-' + statC.index}")
                                .input-group(th:if="${compareByteSeq?.offset != null && compareByteSeq?.offset != byteSeq?.offset}")
                                  button.incoming Accept incoming
                                  input.short(th:value="${compareByteSeq.offset != null && compareByteSeq?.offset != byteSeq?.offset}", th:id="${'ByteSequenceMinOffset' + stat.index + '-' + statB.index + '-' + statC.index}")
                              
                              //- Max offset
                              .form-row.max-offset(th:classappend="${compareByteSeq?.maxOffset != null && compareByteSeq?.maxOffset != byteSeq?.maxOffset} ? 'before-review' : ''")
                                p.inputType(th:text="${#strings.isEmpty(byteSeq?.maxOffset) ? 'New content' : 'Edited content'}") Edited content
                                .tooltipPlusAction
                                  .tooltip-container
                                    .label
                                      label.from-external-only(th:for="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index + '-' + statC.index}") Max offset
                                      label.from-internal-only(th:for="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index + '-' + statC.index}") Max offset (required)
                                      span.tooltip-toggle(aria-label=htmlWebpackPlugin.options.getTooltip('form', '2.4.6') tabindex='0')
                                        .i-wrapper
                                          i.fa.fa-solid.fa-circle-info(role="img" aria-label="information icon")
                                  .action-monitoring
                                    .review-type
                                      i.fa-solid.fa-triangle-exclamation(aria-hidden="true")
                                      p Require reviewing
                                    .review-type
                                      i.fa-solid.fa-square-check
                                      p Reviewed
                                .input-group
                                  button.current Accept current
                                  input.short(th:field="*{containerSignatures[__${stat.index}__].files[__${statB.index}__].byteSequences[__${statC.index}__].maxOffset}", th:id="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index + '-' + statC.index}")
                                .input-group(th:if="${compareByteSeq?.maxOffset != null && compareByteSeq?.maxOffset != byteSeq?.maxOffset}")
                                  button.incoming Accept incoming
                                  input.short(th:value="${compareByteSeq.maxOffset}" , th:id="${'ByteSequenceMaxOffset' + stat.index + '-' + statB.index + '-' + statC.index}")

                          .button-container
                            button.black.small.add-byte-sequence.push-right
                              | Add byte sequence

                    .button-container
                      button.black.small.push-right.add-path
                        | Add path